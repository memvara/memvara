"""Benchmark packages that ship with this repository.

`benchmarks.agent_memory` is the public Agent Memory Benchmark: a dataset, an adapter
interface and a scorer that any memory system can run. It is deliberately separate from
`bench/`, which is this project's own regression and comparison harnesses and imports
its modules by bare name.
"""
